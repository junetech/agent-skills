interface:
  display_name: "Review PR"
  short_description: "Review PR or branch changes without editing by default"
  default_prompt: "Use $review-pr to review the current PR or branch against its base and report actionable findings without modifying files."
